# ==============================================
# AgroTributos API - Configurações Backend
# ==============================================
# Copie este arquivo para .env e preencha com seus valores

# ----------------------------------------------
# SERVIDOR
# ----------------------------------------------
# Porta onde a API será executada
PORT=3001

# Ambiente de execução (development | production | test)
NODE_ENV=development

# ----------------------------------------------
# OPENAI API (OBRIGATÓRIO)
# ----------------------------------------------
# Chave de API da OpenAI para o assistente IA
# Obtenha em: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-your-api-key-here

# ----------------------------------------------
# SUPABASE (OPCIONAL - Futuro)
# ----------------------------------------------
# URL do projeto Supabase
# Obtenha em: https://app.supabase.com/project/_/settings/api
SUPABASE_URL=https://your-project.supabase.co

# Chave anônima (anon/public key) do Supabase
SUPABASE_KEY=your-supabase-anon-key-here

# ----------------------------------------------
# CORS (OPCIONAL)
# ----------------------------------------------
# URLs permitidas para CORS (separadas por vírgula)
# Exemplo: http://localhost:3000,https://app.agrotributos.com.br
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ----------------------------------------------
# LOGS E DEBUG (OPCIONAL)
# ----------------------------------------------
# Nível de log (error | warn | info | debug)
LOG_LEVEL=info

# Ativar logs detalhados da OpenAI (true | false)
DEBUG_OPENAI=false

# ----------------------------------------------
# BANCO DE DADOS (PostgreSQL)
# ----------------------------------------------
# String de conexão do PostgreSQL
# Formato: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
# Exemplo local: postgresql://postgres:postgres@localhost:5432/agrotributos
# Exemplo Supabase: postgresql://postgres:[PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/agrotributos"

# ----------------------------------------------
# WHATSAPP (WPPConnect Server)
# ----------------------------------------------
# URL base do servidor WPPConnect
WPP_BASE_URL=http://localhost:21465

# Nome da sessão do WhatsApp
WPP_SESSION=NERDWHATS_AMERICA

# Chave secreta para gerar tokens
WPP_SECRET_KEY=THISISMYSECURETOKEN

# Token de autenticação (gerado pelo endpoint /generate-token)
WPP_TOKEN=
